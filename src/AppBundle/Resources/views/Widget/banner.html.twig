{% if banner %}
    <br />
    <a href="{{ banner.url }}" target="_blank"><img src="{{ banner.file.path }}" style="width: 240px; border: 1px solid #CCC;" title="{{ banner.title }}" alt="{{ banner.title }}" ></a>
{% endif %}