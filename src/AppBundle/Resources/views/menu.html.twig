<nav class="navbar navbar-black-default navbar-black">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#menu" aria-expanded="false">
            <span class="sr-only">Навигация</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
        </button>
        <!--<a class="navbar-brand" href="/" id="logo2"><img src="images/logo2.png"></a>-->
    </div>

    <div class="collapse navbar-collapse" id="menu">
        <ul class="nav navbar-nav">
            <li class="nomenu"></li>
            {% for m in menu %}
            {#<li class="active"><a href="">Главная</a></li>#}
            <li>
                {#<span class="fa fa-angle-down"></span>#}
                <a href="{{ m.url }}">{{ m.title }} {{ m.children | length > 0 ? ' ' }}</a>
                {% if m.children | length > 0 %}
                    <div class="submunu">
                        <ul>
                            {% for sm in m.children %}
                                <li><a href="{{ m.url }}{{ sm.url }}">{{ sm.title }}</a></li>
                            {% endfor %}
                        </ul>
                    </div>
                {% endif %}
            </li>
            {% endfor %}
            <li class="nomenu"></li>
        </ul>
    </div><!-- /.navbar-collapse -->
</nav>