    <link href="{{ asset('bundles/admin/tapmodo-Jcrop/css/jquery.Jcrop.min.css') }}" rel="stylesheet" media="all">
    <script src="{{ asset('bundles/admin/tapmodo-Jcrop/js/jquery.Jcrop.min.js') }}" type="text/javascript"></script>
    <script src="{{ asset('bundles/admin/js/file.js') }}"></script>

    <div class="">
        <h3>Галерея публикации</h3>
        <div class="row">
            {% for k,image in galery %}
                {% if (k) % 4  == 0 %}
                    </div>
                    <div class="row">
                {% endif %}
                <div class="col-xs-4">
                    <div class="thumbnail">
                        <img src="{{ image.image.thumbnail }}" alt="{{ image.title }}" title="{{ image.title }}">
                        <div class="caption">
                            <h4>{{ image.title }}</h4>
                            <p><a href="{{ path('admin_publication_edit_galery_remove',{'id' : publication.id , 'imageId' : image.id }) }}" class="btn btn-danger" role="button">Удалить</a></p>
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
        <form method="post" enctype="multipart/form-data" class="form-horizontal" action="{{ path('admin_publication_edit_galery_save',{'id' : publication.id }) }}">
            <div class="form-group">
                <label class="col-sm-4 control-label required">Название файла</label>
                <div class="col-sm-8">
                    <input type="text" class="title form-control" name="title">
                </div>
            </div>
            <div class="form-group">
                <label class="col-sm-4 control-label required">Выберите файл</label>
                <div class="col-sm-8">
                    <div class="file-container">
                        <input type="file" name="ImageFile" id="ImageFile" class="fileAjax" {{ edit is not defined ? 'required' : '' }} >
                        <br />
                        <br />
                        <div class="fileDoc">
                            <img src="" brightness="0" contrast="0">
                        </div>
                        <div class="navigateFile">
                            <span class="glyphicon glyphicon-chevron-left rotateLeft">&nbsp;</span>
                            <div style="cursor: pointer; border: 1px solid #CCC; border-radius: 5px; display: inline-block" >
                                <span class="cropImage minibtn" title="Обрезать изображение">Обрезать</span>
                                <br />
                                <br />
                            </div>
                            <span class="glyphicon glyphicon-chevron-right rotateRight">&nbsp;</span>
                        </div>
                        <input type="hidden" id="x1" name="x1">
                        <input type="hidden" id="y1" name="y1">
                        <input type="hidden" id="x2" name="x2">
                        <input type="hidden" id="y2" name="y2">
                    </div>
                </div>
            </div>
            <div class="form-group">
                <div class="col-sm-8 col-sm-offset-4">
                    <button type="submit" class="btn btn-primary">Сохранить</button>
                </div>
            </div>
        </form>
    </div>