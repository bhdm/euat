{% extends "AdminBundle::layout.html.twig" %}

{% block breadcrumb %}
    <ul class="breadcrumb well">
        <li><a href="{{ path('admin_main') }}">Домой</a> <span class="divider"></span></li>
        <li class="active"><a href="{{ path('admin_seopage_list') }}">Страницы</a> <span class="divider"></span></li>
        <div style="float: right">
            <a href="{{ path('admin_seopage_add') }}">Добавить</a> <span class="divider"></span>
        </div>
    </ul>
{% endblock %}

{% block content %}
    <div class="well">
        <table class="table">
            <tr>
                <th>№</th>
                <th>URL</th>
                <th class="setting">Инструменты</th>
            </tr>
            {% for item in pagination %}
                <tr>
                    <td>{{ item.id }}</td>
                    <td>{{ item.url }}</td>
                    <td class="setting">
                        {% spaceless %}
                        <a href="{{ path('admin_seopage_edit',{'id' : item.id}) }}">
                            <button type="button" class="btn btn-default btn-sm">
                                <span class="glyphicon glyphicon-pencil"></span>
                            </button>
                        </a>
                            &nbsp;
                            &nbsp;
                        <a href="#" data-href="{{ path('admin_seopage_remove',{'id' : item.id}) }}" class="remove" data-toggle="modal" data-target="#confirm-delete">
                            <button type="button" class="btn btn-default btn-sm">
                                <span class="glyphicon glyphicon-remove"></span>
                            </button>
                        </a>
                        {% endspaceless %}
                    </td>
                </tr>
            {% endfor%}
        </table>
        <nav>
            {{ knp_pagination_render(pagination) }}
        </nav>
    </div>
{% endblock %}